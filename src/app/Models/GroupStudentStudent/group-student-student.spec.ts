import { GroupStudentStudent } from './group-student-student';

describe('GroupStudentStudent', () => {
  it('should create an instance', () => {
    expect(new GroupStudentStudent()).toBeTruthy();
  });
});
